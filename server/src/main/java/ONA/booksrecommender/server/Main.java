package ONA.booksrecommender.server;

public class Main {

    public static void main(String[] args) {
        /*System.out.println("Server BooksRecommender in avvio...");
        // Qui puoi:
        // - inizializzare un HTTP server (es. con SparkJava, Spring Boot, ecc.)
        // - caricare dati, inizializzare thread di ascolto sulle porte, ecc.
        // Per ora un semplice placeholder:
        try {
            Thread.sleep(Long.MAX_VALUE);
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            System.err.println("Server interrotto: " + e.getMessage());
        }*/
        Server.main(args);
    }
}
